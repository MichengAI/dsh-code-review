# I002 Codex 原版审查规范

- 状态：已完成
- 负责人：Codex 开发任务
- 优先级：高
- 更新时间：2026-09-13 13:28（Asia/Shanghai）
- 关联提交：未提交

## 目标与边界

用户要求保留 Codex 原味，减少跨平台提示词差异。将首版自行压缩的中文审查规范替换为固定提交的完整英文 rubric，保持原始字节；不承诺不同模型和工具环境产生相同结论。

沿用尚未发布的 0.1.0 包版本；不提交、推送、发布或安装到日常 profile，不修改专家插件。

## 实现

- 上游：openai/codex，提交 `a592c38c16cdd7623dacc9168926ebccedfb67d3`，路径 `codex-rs/prompts/templates/review/rubric.md`。
- 原文：`assets/codex/review/rubric.md`；Git blob 为 `85e89cb7eeadb97fa9e4868d2b9977c87225506d`，SHA-256 为 `ec60e7f36a1d1c2679ce095c0205ecc56f7dd8fb57707a13ef362072390f219f`。元数据另存 source.json，.gitattributes 禁止自动换行转换。
- `src/prompt.ts` 将完整原文直接作为运行提示词前缀；中文 DSH 适配独立追加，只说明冻结工具、语言和协议扩展，不添加专家清单或改写审查判断标准。
- 恢复 Codex 原生报告字段、优先级可空/省略、单条与整体置信度及整体正确性；不按置信度过滤，也不将其描述为实测准确率。
- 可选扩展仅为 `code_location.side` 与顶层 `limitations`。原文证据由宿主提取，不再要求自定义 evidence 字段；沿用上游行号长度建议，不额外硬拒绝超过 10 行的有效定位。
- 内部旧报告格式仍可渲染，新模型输出采用原生字段。现有只读工具、生命周期及异常状态保持原有行为。

## 任务与验收

- [x] 从固定 GitHub 提交获取原文，保存来源和校验值，保留许可归属。
- [x] 先确认原文与报告协议测试失败，再实现并通过新契约测试。
- [x] 更新模型请求，确认完整原文进入真实 DSH AgentLoop 请求；更新原生报告与旧记录兼容测试。
- [x] 完成最终源码回归、文档检查、隔离包安装与打包后请求验证。
- [x] 归档并更新受影响链接。

## 阻塞与后续

本轮实现无阻塞。在线同模型/同快照的 Codex 与 DSH 对照评测尚未执行，继续由交接入口待办承接。I001 的空 profile 清理遗留不在本次修改范围内。

阅读顺序：本总览 → 原文及 source.json → src/prompt.ts → src/report.ts → tests/codex-prompt.test.mjs、tests/host.test.mjs。

## 验收结果

- `npm run check`：严格 TypeScript 构建、28 项测试全部通过，无跳过；文档本地链接校验通过。
- `npm run verify:package`：项目内隔离 DSH_HOME 安装、完整 peer 检查、15 项安装后宿主测试通过；其中包含从包内加载原文，并确认原文完整进入模型请求。
- 新增原文一致性与原生 schema 测试先在旧实现上失败，再在新实现上通过；既有报告渲染兼容测试通过。
- 更新 README、NOTICE 与打包文件清单；原文、来源元数据及独立适配实现随包交付。归档后重新打包并进行逐文件字节一致性检查。
- 在线跨平台模型质量对照未执行，参见[待办与阻塞](../../../00-交接入口/03-待办与阻塞.md)。未提交、推送、发布或安装到日常 profile。
