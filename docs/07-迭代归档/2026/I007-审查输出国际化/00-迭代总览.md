# I007 审查输出国际化

状态：已完成

负责人：Codex

优先级：P1

更新时间：2026-09-13 18:42（Asia/Shanghai）

关联提交：`feat: follow host locale for review output`

## 目标与边界

保留 Codex 英文 rubric 及短任务原文，审查报告输出语言跟随宿主中英文设置。复用专家插件读取 `locale.preference` 的入口，通过原生 systemPrompt context 传递语言约束，不新增配置页面或语言配置。

## 已完成与验收

- [x] 每轮开始捕获语言，范围菜单、子 Agent 和运行反馈保持一致；切换设置后下一轮生效。
- [x] 缺少 settings 或偏好时默认中文，英文区域标签兼容 `en-*`；仅浏览器自动语言不可从后端获取。
- [x] 原版 rubric 字节不变；JSON 协议枚举、路径与优先级保持原样，历史正文不翻译。
- [x] 回归先复现英文宿主仍返回中文状态；修复后 43 项源码测试通过，包含实际 AgentLoop 请求中的语言指令、切换语言和报告保存。
- [x] 保留 I006 工具作用域修复及对应回归。

## 遗留与恢复

在线模型与 Desktop/Web 实际语言验收由[待办与阻塞](../../../00-交接入口/03-待办与阻塞.md)承接。离线模型只验证指令和数据链路，不证明真实模型始终遵循语言要求。命令目录元数据在加载时取语言，重新加载后更新。

回退可重新安装根目录保留的 `michengai-dsh-code-review-0.1.0-scope-d0ab4a12.tgz` 并重启 DSH，不删除会话或报告。本轮已按用户要求分批提交；未推送。

## 阅读顺序

[README](../../../../README.md) → src/i18n.ts → src/selection.ts、src/runtime.ts、src/index.ts → tests/host.test.mjs。
