# I003 Web 审查反馈

- 状态：已完成
- 负责人：Codex 开发任务
- 优先级：高
- 更新时间：2026-09-13 14:54（Asia/Shanghai）
- 关联提交：未提交

## 目标与边界

修复本机测试中命令输入受限、无效路径误报无变更，以及主会话区域不显示审查反馈的问题。沿用未发布的 0.1.0，更新本机 web profile，不改动其他插件或官方宿主文件。

## 实现与证据

- 增加 `--worktree`，保留 `.` 入口；无效路径明确报错，已删除或未变化的有效路径仍可审查。
- 本机命令日志证实 `/review .` 被接收，独立模型执行过读取和搜索，曾在 180 秒后超时；没有反馈不等于未执行。
- rc.2 官方 Chat 的 `isActive` 排除普通 command 节点。纯命令新会话保持 blank，审查子 Agent 的 turn 不改变父会话状态。
- 新增前端专属 `michengai-review` 节点，投影已有 command/run 与 command/done；展示运行、成功和失败文字，并隐藏重复通用卡片。无需写新事件或伪造主会话消息。
- 注册浏览器模块，React 由宿主提供，前端构建随 npm 包交付。

## 任务与验收

- [x] 复现输入与路径错误，增加回归覆盖。
- [x] 使用官方真实 Conversation 组装器与 Chat 投影复现纯命令 blank，验证新卡片进入 active。
- [x] 验证成功、超时、刷新回放和其他命令不受影响；从实际前端产物加载注册与渲染函数。
- [x] 严格 TypeScript、32 项回归测试与文档检查通过。

## 后续与限制

浏览器自动化连接不可用，真实页面视觉验收由维护者本机继续验证；180 秒审查超时未在此迭代调整。该项与在线模型质量统一由[待办与阻塞](../../../00-交接入口/03-待办与阻塞.md)承接。

阅读顺序：本总览 → README → src/client-state.ts、src/client.ts → tests/client.test.mjs。
